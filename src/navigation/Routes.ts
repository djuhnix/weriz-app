export enum Routes {
  root = '/',
  rootLogin = '/login',
  rootSignup = '/signup',
  rootForgotPassword = '/forgot-password',
  rootResetPassword = '/reset-password/:token',
  rootLanding = '/landing',
  rootFeed = '/feed',
  login = 'login',

  signup = 'signup',
  landing = 'landing',
  forgotPassword = 'forgot-password',
  resetPassword = 'reset-password/:token',
  feed = 'feed',
}
